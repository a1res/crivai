# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js build output
/.next/
/out/
/build
next-env.d.ts

# Production
*.tsbuildinfo

# Testing
/coverage

# Environment files (never commit secrets — the LLM API key lives in
# `wrangler secret put`, not in the repo. See CLAUDE.md section 5.)
.env
.env.*
!.env.example

# Cloudflare Worker / Wrangler
/worker/node_modules
/worker/.wrangler
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
*.pem
Thumbs.db

# Setup-process artifacts — not part of the project itself
prompt-inicial-claude-code.md
prompt-brainstorm-nomes.md
